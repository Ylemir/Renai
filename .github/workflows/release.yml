name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-package:
    name: Build Python package
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: "pyproject.toml"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Build wheel and sdist
        run: uv build

      - name: Upload Python artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: dist/*
          if-no-files-found: error

  build-binaries:
    name: Build standalone binaries
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: "pyproject.toml"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Build executable
        run: uv run pyinstaller renai.spec

      - name: Package binary
        shell: bash
        run: |
          python - <<'PY'
          import os
          import pathlib
          import shutil

          dist_dir = pathlib.Path("dist")
          if not dist_dir.exists():
              raise SystemExit("dist not found; PyInstaller build failed.")

          artifacts_dir = pathlib.Path("artifacts")
          artifacts_dir.mkdir(exist_ok=True)

          os_name = "${{ matrix.os }}".replace("-latest", "")
          archive = artifacts_dir / f"renai-{os_name}.zip"
          if archive.exists():
              archive.unlink()

          shutil.make_archive(archive.with_suffix(""), "zip", dist_dir)
          PY

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: renai-${{ matrix.os }}
          path: artifacts/*.zip
          if-no-files-found: error

  release:
    name: Publish Release
    needs:
      - build-package
      - build-binaries
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
